type Contact {
    name: String
    role: String
    email: String
    phone: String
}

input ContactInput {
    organizationId: Int!
    name: String
    role: String
    email: String
    phone: String
}

extend type Mutation {
    updateContact(id: ID!, details: ContactInput!): Contact
}
