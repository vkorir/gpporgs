type Contact {
    id: Int
    name: String
    role: String
    email: String
    phone: String
}

input ContactInput {
    id: Int
    name: String
    role: String
    email: String
    phone: String
}

extend type Mutation {
    updateContact(id: ID!, details: ContactInput!): Contact
}
